// Calculate how much Kristina is lazy on weekends. And how fast she goes from one side of the bed to another side of the bad.

tvshow = 30
function lazyass(tvshow){

     let fromAtoB = 1.5
     let lazyAssSpeed = fromAtoB / tvshow
     if ( tvshow < 30){
          return lazyAssSpeed *100
     } if (tvshow < 45) {
          return lazyAssSpeed *100
     }else {
          return lazyAssSpeed * 100
     }
     
}

